## 12. The range lookup problem

Write a program that takes as input a binary search tree and an interval and returns the binary search tree keys that lie in the interval.

> Hint: How many edges are traversed when the successor function is repeatedly called m times?